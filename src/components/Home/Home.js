import React from 'react';
import useCart from '../../hooks/useCart';
import UseTitle from '../useTitle/UseTitle';

const Home = () => {

    return (
        <div>
            <UseTitle title="home"></UseTitle>
            <h1>This is home</h1>
        </div>
    );
};

export default Home;