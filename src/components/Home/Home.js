import React from 'react';
import useCart from '../../hooks/useCart';

const Home = () => {

    return (
        <div>
            <h1>This is home</h1>
        </div>
    );
};

export default Home;